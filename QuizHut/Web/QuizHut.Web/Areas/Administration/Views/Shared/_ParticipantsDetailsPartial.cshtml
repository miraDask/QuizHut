@model ParticipantViewModel
@{
    ViewData["Title"] = "_ParticipantsDetailsPartial";
}

<div class="form-group flex-grow-1 d-inline">
    <nav class="navbar navbar-expand-lg  bg-light navbar-dark p-0">
        <div class="container">
            <div class="navbar-collapse text-center collapse">
                <ul class="navbar-nav ml-auto">
                    <li class="nav-item m-1">
                        <a class="nav-link  btn btn-primary" href="/Answers/Delete?id=@Model.Id">Delete</a>
                    </li>
                </ul>
            </div>
        </div>
    </nav>
    <input type="text" class="form-control form-control-lg flex-grow-1 mr-2 d-inline text-truncate" disabled asp-for="@Model.FullName" value="@Model.FullName @Model.Email">
</div>

